Define:
  +Type: Object,
  Version: String,
  Requirements:
    +Type: Map,
    +KeyType: String,
    +ValueType:
      +Type: String,
      +None: True,
      +Regex: (auto|(==|>=|<=)[a-zA-Z0-9_.-]+)

``` 
+Object:
  vehicles:
    +Object:
      list:
        +list:
          +Object:
            name:
              +String:
                regex: null
            maker:
              +String:
                regex: ^[A-Z][a-z]
            year:
              +Inetger:
                minimum: null
  age:
    +Inetger:
      minimum: 20
  children:
    +Inetger:
      minimum: 2
  name:
    +String:
      regex: ^[A-Z][a-z]
```
